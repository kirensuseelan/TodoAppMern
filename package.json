
{
  "name": "todomern-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "chai": "^3.5.0",
    "chai-http": "^1.0.0",
    "mocha": "^5.2.0"
  },
  "scripts": {
    "start": "node Server/server",
    "build": "cd client/ && npm install && npm run build",
    "test": "mocha Server/test/test-server.js --exit"
  },
   "cacheDirectories": [
    "Client/node_modules",
    "server/node_modules"
  ],
}
